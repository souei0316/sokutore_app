<h1>トレーニング追加</h1>
<% if current_user.id == 1 %>
<%= form_with model: @training, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>トレーニング名</h4>
  <%= f.text_field :title %>
  <h4>説明</h4>
  <%= f.text_area :content %>
  <%= f.submit '投稿' %>
<% end %>
<% else %>
権限がありません。
<% end %>